package httpHandler

/*
I couldn't put the request handler function here because I need the
variable dfp and I can't pass its adress to requestHandler since it has
to be compatible with http.HandleFunc.
*/
